const Routes = Object.freeze({

    // BASIC ROUTES 
    LANDING: "/",
    CONTACT: "/contact",
    REGISTER: "/register",
    LOGIN: "/login"

    // AUTHENTIFICATED ROUTES
});

export default Routes;